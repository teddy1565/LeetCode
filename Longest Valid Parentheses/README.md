# 解題紀錄簡筆

## 解題思路流程

最一開始打算利用多個索引的移動，模擬stack的行為，來記錄目前有效括號的長度

後來發現，有一個很重要的點，長度的計算似乎有相關聯

所以改用stack來記錄目前有效括號的起始位置

要直接利用索引與長度，計算有效括弧的長度

```c
// )())))()()
stack = [-1, 1]
```

計算長度，是i - stack[stack_index - 1]，更新時則是拿掉stack的最後一個元素
